apiVersion: v1
kind: Pod
metadata:
  name: my-pod9
  labels:
    app: my-app
spec:
  containers:
  - name: my-container
    image: ubuntu
    ports:
    - containerPort: 80
      protocol: TCP
    # Add additional container configurations if needed
    # env:
    # - name: ENV_VARIABLE
    #  
